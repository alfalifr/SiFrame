<?xml version="1.0" encoding="utf-8"?>
<font-family xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- regular -->
    <font
        android:font="@font/sf_pro_regular"
        android:fontStyle="normal"

        app:font="@font/sf_pro_regular"
        app:fontStyle="normal" />

    <!-- regular -->
    <font
        android:font="@font/sf_pro_medium"
        android:fontStyle="normal"

        app:font="@font/sf_pro_medium"
        app:fontStyle="normal" />

    <!-- light -->
    <font
        android:font="@font/sf_pro_light"
        android:fontStyle="normal"

        app:font="@font/sf_pro_light"
        app:fontStyle="normal" />

    <!-- bold -->
    <font
        android:font="@font/sf_pro_bold"
        android:fontStyle="normal"

        app:font="@font/sf_pro_bold"
        app:fontStyle="normal"/>

    <!-- semi bold -->
    <font
        android:font="@font/sf_pro_semibold"
        android:fontStyle="normal"

        app:font="@font/sf_pro_semibold"
        app:fontStyle="normal"/>
</font-family>